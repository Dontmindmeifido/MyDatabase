# MyDB

Dependencies: Dear ImGui, GLFW (Graphics Library Framework)-dev toolkit.

// TODO TODAY, 
add simple where (comparison =, >, < not supporting logical operations) and order by (ascending, descending) clauses,
make syntax highlighting (highlight actions, sources and destinations),  // DONE
make syntax suggestions, 
make it that when you double click, you can update cell value

OPTIONAL
add enforced Datatypes and verification in headers, (ex. impossible number to datetime) // DONE
maybe when you hover over a cell you get the datatype and row+column number, // DONE

Enforce project requirements.